# Bright Data Proxy Diagnostics — Руководство

**Автоматическая диагностика и мониторинг здоровья Bright Data Proxy**

---

## 🚀 Быстрый запуск

### Через Playbook

```bash
@playbook proxy-diagnostics
```

### Напрямую через Python

```bash
python scripts/proxy_diagnostics.py
```

---

## 📊 Что проверяется

### 1. Connectivity (Подключение)
- Доступность proxy сервера
- Latency (задержка)
- IP адрес через proxy

### 2. Geolocation (Геолокация)
- Страна и город proxy IP
- Регион и временная зона
- ISP (провайдер)

### 3. Speed (Скорость)
- Скорость загрузки (KB/s)
- Время подключения
- Время первого байта

### 4. JavaScript Rendering
- Способность обрабатывать JS страницы
- Latency при рендеринге
- Длина контента

---

## 📄 Отчёты

После диагностики создаются два отчёта:

### JSON отчёт
`.cursor/audit/proxy_diagnostics.json`

Структурированные данные для автоматической обработки.

### Markdown отчёт
`.cursor/audit/proxy_diagnostics.md`

Человекочитаемый отчёт с детальными результатами.

---

## 📈 Интерпретация результатов

### Overall Status

| Статус | Значение | Действие |
|--------|----------|----------|
| **healthy** | Все проверки пройдены | ✅ Система работает нормально |
| **degraded** | Часть проверок провалена | ⚠️ Требуется внимание |
| **unhealthy** | Большинство проверок провалено | ❌ Требуется исправление |

### Метрики качества

**Latency:**
- < 500ms — отлично
- 500-1000ms — хорошо
- 1000-2000ms — приемлемо
- > 2000ms — проблемы

**Download Speed:**
- > 100 KB/s — отлично
- 50-100 KB/s — хорошо
- 20-50 KB/s — приемлемо
- < 20 KB/s — медленно

---

## 🔄 Автоматический мониторинг

### Добавление в cron

Для ежедневной проверки:

```bash
# Каждый день в 03:00
0 3 * * * cd /path/to/reflexio && python scripts/proxy_diagnostics.py
```

### Интеграция с Monitoring Agent

Зарегистрируйте диагностику для автоматического мониторинга:

```python
from src.osint.monitoring_agent import MonitoringAgent
from pathlib import Path

# Создайте playbook для диагностики proxy
agent = MonitoringAgent()
# Можно добавить в расписание
```

---

## 🧪 Ручная проверка подключения

Для быстрой проверки вручную:

```bash
# Получение IP через proxy
curl -x https://brd-customer-hl_16abad82-zone-tttt:46ju8s7m4bcz@brd.superproxy.io:9515 https://api.ipify.org
```

Если видите IP адрес — proxy работает!

---

## 🔧 Решение проблем

### Проблема: Connection timeout

**Возможные причины:**
- Proxy сервер недоступен
- Неправильные credentials
- Сетевые проблемы

**Решение:**
1. Проверьте credentials в `.env`
2. Проверьте доступность `brd.superproxy.io`
3. Проверьте firewall настройки

### Проблема: Proxy error / Authentication failed

**Возможные причины:**
- Неправильный пароль
- Истёк срок действия credentials
- Неправильный формат URL

**Решение:**
1. Убедитесь, что формат правильный: `https://brd-customer-USERNAME-zone-ZONE:PASSWORD@brd.superproxy.io:PORT`
2. Проверьте credentials в панели Bright Data
3. Обновите `.env` файл

### Проблема: Slow speed

**Возможные причины:**
- Перегрузка proxy сервера
- Географическое расстояние
- Сетевые проблемы

**Решение:**
1. Проверьте нагрузку в панели Bright Data
2. Попробуйте другую зону (если доступна)
3. Запустите диагностику в разное время

---

## 📊 Метрики в cursor-metrics.json

После диагностики метрики автоматически обновляются:

```json
{
  "metrics": {
    "mcp": {
      "brightdata_proxy_status": "healthy",
      "brightdata_latency_ms": 245.3,
      "brightdata_download_speed_kbps": 87.5,
      "brightdata_last_check": "2025-11-03T21:00:00Z"
    }
  }
}
```

Эти метрики учитываются в:
- Governance Loop для оценки здоровья системы
- Audit Reports для оценки зрелости
- MCP Validation для проверки доступности

---

## ✅ Рекомендации

### Еженедельная проверка
Запускайте диагностику раз в неделю для мониторинга здоровья:

```bash
@playbook proxy-diagnostics
```

### Перед важными миссиями
Перед запуском важных OSINT миссий проверьте proxy:

```bash
python scripts/proxy_diagnostics.py
```

### При проблемах
Если OSINT миссии не работают, запустите диагностику для выявления проблемы.

---

**Proxy Diagnostics готов к использованию!** 🎯✨













