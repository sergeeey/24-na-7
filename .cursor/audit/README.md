# CEB-E Audit Pack v1.0 — Эталонный стандарт

Эталонный аудит проекта по стандарту **Cursor Enhancement Blueprint Evaluation v1.0**.

**CEB-E v1.0** — комплексная система оценки зрелости AI-Native инженерной среды, проверяющая все 9 ключевых компонентов и выдающая количественную оценку с уровнем зрелости (0-5).

Подробнее: [CEB-E_STANDARD.md](CEB-E_STANDARD.md)

## 📋 Описание

CEB-E Audit Pack превращает проект в **самопроверяемую инженерную среду**, которая автоматически оценивает уровень зрелости проекта и настраивает governance профиль.

## 🚀 Использование

### Быстрый запуск через Playbook

```bash
@playbook audit-standard
```

### Ручной запуск

```bash
# Эталонный аудит (рекомендуется)
python .cursor/audit/run_audit.py --mode standard

# Быстрый аудит (только структура)
python .cursor/audit/run_audit.py --mode quick

# Полный аудит (со всеми проверками)
python .cursor/audit/run_audit.py --mode full
```

### Генерация отчёта

После выполнения аудита:

```bash
python .cursor/audit/generate_report.py
```

Отчёт будет сохранён в `.cursor/audit/audit_report.md`.

### Применение Governance

```bash
python .cursor/metrics/governance_loop.py --apply results
```

## 📊 Что проверяется (9 компонентов, 100 баллов)

### Эталонная структура CEB-E v1.0

1. **Rules Engine** (15) — `.cursor/rules/` — структура правил, frontmatter
2. **Memory Bank 2.0** (10) — `.cursor/memory/` — автообновление контекста
3. **MCP Gateway** (10) — `.cursor/mcp.json` — валидность и доступность
4. **Hooks System** (10) — `.cursor/hooks.json` — реактивная автоматизация
5. **Validation Framework** (15) — `.cursor/validation/` — SAFE+CoVe валидаторы
6. **Observability** (10) — `.cursor/metrics/` — сбор метрик и интеграция
7. **Governance Loop** (10) — `.cursor/governance/` — автопереключение профиля
8. **Playbooks Suite** (10) — `.cursor/playbooks/` — ключевые плейбуки
9. **Multi-Agent System** (10) — `.cursor/agents/` — изоляция агентов

## 🎯 Уровни зрелости

| Уровень | Баллы | Название | Описание |
|---------|-------|----------|----------|
| 1 | 0-19 | Initial | Базовые компоненты отсутствуют |
| 2 | 20-39 | Managed | Основные компоненты на месте |
| 3 | 40-59 | Defined | Структура определена, есть валидация |
| 4 | 60-79 | Quantitatively Managed | Метрики и governance работают |
| 5 | 80-100 | Optimizing | Полная самопроверяемая среда |

## 📁 Структура файлов

```
.cursor/
├── audit/
│   ├── README.md                    # Этот файл
│   ├── run_audit.py                 # Основной скрипт аудита
│   ├── generate_report.py           # Генератор отчётов
│   ├── audit_report_template.md     # Шаблон markdown отчёта
│   ├── audit_report.json            # JSON результаты (генерируется)
│   └── audit_report.md              # Markdown отчёт (генерируется)
├── metrics/
│   └── governance_loop.py           # Применение governance профиля
└── playbooks/
    └── audit.yaml                   # Playbook для запуска аудита
```

## 🔧 Зависимости

Для работы требуется:

- Python 3.11+
- `pyyaml` (для governance_loop.py)

Установка зависимостей:

```bash
pip install pyyaml
```

## 📈 Метрики

Аудит вычисляет следующие метрики:

- **Общий балл** — сумма всех проверенных компонентов
- **AI Reliability Index** — процент надёжности системы для AI-агентов
- **Context Hit Rate** — эффективность использования контекста
- **Уровень зрелости** — оценка по 5-балльной шкале

## 🎛️ Governance профили

В зависимости от уровня зрелости автоматически выбирается профиль:

- **self-adaptive** (Level 5) — полная автоматизация
- **optimizing** (Level 4) — оптимизация с метриками
- **managed** (Level 3) — управляемые процессы
- **basic** (Level 2) — базовая конфигурация
- **initial** (Level 1) — требуется настройка

Профиль сохраняется в `.cursor/governance/profile.yaml`.

## 🔄 Интеграция с CI/CD

Аудит можно интегрировать в CI/CD пайплайн:

```yaml
# .github/workflows/audit.yml
- name: Run CEB-E Audit
  run: python .cursor/audit/run_audit.py --mode full

- name: Generate Report
  run: python .cursor/audit/generate_report.py

- name: Upload Report
  uses: actions/upload-artifact@v3
  with:
    name: audit-report
    path: .cursor/audit/audit_report.md
```

## 📝 Расширение

Для добавления новых проверок отредактируйте `run_audit.py` и добавьте новые функции проверки, затем обновите шаблон отчёта в `audit_report_template.md`.

