# Reflexio 24/7 — Production Environment Template
# ИНСТРУКЦИЯ:
#   1. Скопируй этот файл на VPS: cp .env.prod.example .env
#   2. Замени все значения <...> на реальные
#   3. НИКОГДА не коммить .env в git (уже в .gitignore)

# ───── Core ─────────────────────────────────────────
DB_BACKEND=sqlite
SAFE_MODE=audit
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000

# ───── LLM API Keys ──────────────────────────────────
# Получить: https://platform.openai.com/api-keys
OPENAI_API_KEY=<твой_новый_openai_ключ>

# Получить: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=<твой_новый_anthropic_ключ>

LLM_PROVIDER=anthropic
LLM_MODEL_ACTOR=claude-3-haiku-20240307
LLM_MODEL_CRITIC=claude-3-haiku-20240307
LLM_TEMPERATURE_ACTOR=0.3
LLM_TEMPERATURE_CRITIC=0.0

# ───── Audio ─────────────────────────────────────────
AUDIO_SAMPLE_RATE=16000
AUDIO_VAD_AGGRESSIVENESS=2
FILTER_MUSIC=true

# ASR: medium — баланс скорость/качество для русского языка
ASR_MODEL_SIZE=medium
ASR_LANGUAGE=ru

# ───── Storage ───────────────────────────────────────
STORAGE_PATH=src/storage

# ───── Redis ─────────────────────────────────────────
RATE_LIMIT_STORAGE=redis
REDIS_URL=redis://redis:6379/0

# ───── Auth ──────────────────────────────────────────
# Сгенерировать новый ключ: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=<сгенерируй_новый_ключ>

# ───── CORS ──────────────────────────────────────────
# Замени на свой реальный домен
CORS_ORIGINS=https://reflexio.duckdns.org
