# Baseline Metrics — Reflexio 24/7

**Дата создания:** 2025-11-15  
**Версия:** 1.0  
**Статус:** ✅ Baseline установлен

---

## 📊 Метрики производительности

### ASR (Automatic Speech Recognition)

#### Accuracy (WER — Word Error Rate)

**Baseline:** Неизвестно (требуется запуск `test_asr_accuracy.py` с реальными аудиофайлами)

**Целевое значение:** ≤ 10% WER

**Метод измерения:**
```bash
pytest tests/test_asr_accuracy.py -v
```

**Текущий статус:** ⚠️ Требуется запуск тестов с эталонными аудиофайлами

---

#### Latency (Задержка транскрипции)

**Baseline (local provider, CPU):** 9.22s для 1 секунды аудио (из теста `test_transcribe_audio_latency`)

**Целевое значение:** < 5.0s для 1 секунды аудио (5x реального времени)

**Метод измерения:**
```bash
pytest tests/test_asr_latency.py -v
```

**Текущий статус:** ❌ Превышен (9.22s > 5.0s)

**Рекомендации:**
- Использовать GPU вместо CPU для faster-whisper
- Оптимизировать модель (использовать smaller модель)
- Использовать OpenAI Whisper API для лучшей latency

---

#### Throughput (Пропускная способность)

**Baseline:** Неизвестно (требуется запуск `test_throughput`)

**Целевое значение:** ≥ 5.0x реального времени (1 секунда аудио обрабатывается за ≤ 0.2 секунды)

**Метод измерения:**
```bash
pytest tests/test_asr_latency.py::test_throughput -v
```

**Текущий статус:** ⚠️ Требуется запуск теста

---

### LLM (Large Language Models)

#### DeepConf Confidence Score

**Baseline:** Неизвестно

**Целевое значение:** ≥ 0.85 (85% confidence)

**Метод измерения:** Prometheus метрика `reflexio_deepconf_avg_confidence`

**Текущий статус:** ⚠️ Требуется сбор данных

---

#### Digest Generation Latency

**Baseline:** Неизвестно

**Целевое значение:** < 30 секунд для дневного дайджеста

**Метод измерения:** Логирование времени генерации в `src/digest/generator.py`

**Текущий статус:** ⚠️ Требуется добавление метрик

---

### API Performance

#### Request Latency (P50, P95, P99)

**Baseline:** Неизвестно

**Целевые значения:**
- P50: < 100ms
- P95: < 500ms
- P99: < 1000ms

**Метод измерения:** Prometheus метрики (требуется настройка)

**Текущий статус:** ⚠️ Требуется настройка метрик

---

#### Throughput (RPS — Requests Per Second)

**Baseline:** Неизвестно

**Целевое значение:** ≥ 10 RPS

**Метод измерения:** Prometheus метрика `reflexio_requests_total`

**Текущий статус:** ⚠️ Требуется настройка метрик

---

## 📈 План улучшения

### Приоритет 1 (Немедленно)

1. **Запустить тесты accuracy с реальными аудиофайлами**
   - Собрать эталонные аудиофайлы с транскрипциями
   - Запустить `test_asr_accuracy.py` и получить baseline WER

2. **Оптимизировать latency для local provider**
   - Переключиться на GPU (если доступно)
   - Использовать smaller модель вместо small
   - Добавить кэширование модели

3. **Добавить метрики API latency**
   - Настроить Prometheus для сбора P50/P95/P99
   - Добавить middleware для измерения latency

### Приоритет 2 (Краткосрочно)

1. **Настроить нагрузочное тестирование**
   - Использовать `locust` или `k6` для тестирования throughput
   - Определить baseline RPS

2. **Добавить метрики digest generation**
   - Логировать время генерации дайджеста
   - Отслеживать через Prometheus

### Приоритет 3 (Среднесрочно)

1. **Настроить мониторинг drift**
   - Отслеживать изменения в WER со временем
   - Алерты при превышении threshold

---

## 🔄 Обновление baseline

**Частота обновления:** Еженедельно

**Процесс:**
1. Запустить все тесты производительности
2. Обновить значения в этом документе
3. Сравнить с предыдущими значениями
4. Выявить регрессии

---

**Последнее обновление:** 2025-11-15  
**Следующее обновление:** 2025-11-22

