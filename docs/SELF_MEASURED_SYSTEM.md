# 🔬 Self-Measured System — Reflexio v2.1

**Дата:** 4 ноября 2025  
**Версия:** Reflexio v2.1 "Surpass Smart Noter"  
**Статус:** ✅ Реализовано

---

## 🎯 Что это?

**Self-Measured System** — это система автоматического измерения и обновления метрик в чеклисте спринта на основе результатов тестов.

Reflexio теперь не просто валидирует себя, но и **автоматически измеряет** свои показатели.

---

## 🏗️ Архитектура

```
┌─────────────────┐
│   Тесты (pytest)│
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  JSON Report    │
│  (.report.json) │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│ auto_measure.py  │
│  (парсинг)       │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│   Checklist      │
│   (YAML)         │
└─────────────────┘
```

---

## 🛠️ Компоненты

### 1. `scripts/auto_measure.py`

**Назначение:** Парсит pytest JSON отчёт и обновляет метрики в чеклисте.

**Функции:**
- `extract_wer_from_tests()` — извлекает WER из тестов
- `extract_latency_from_tests()` — извлекает Latency
- `extract_offline_duration_from_tests()` — извлекает длительность офлайн транскрипции
- `extract_deepconf_from_tests()` — извлекает DeepConf score
- `extract_factual_consistency_from_tests()` — извлекает Factual consistency
- `extract_token_entropy_from_tests()` — извлекает Token entropy

**Использование:**
```bash
# Автоматическое обновление
make update-metrics

# Dry-run (проверка без изменений)
make update-metrics-dry-run
```

### 2. `scripts/measure_metrics.py`

**Назначение:** Запускает тесты напрямую и измеряет метрики в реальном времени.

**Функции:**
- `run_asr_accuracy_test()` — запускает тест WER
- `run_asr_latency_test()` — запускает тест Latency
- `run_offline_test()` — запускает тест офлайн транскрипции

**Использование:**
```bash
# Измерить все метрики
make measure-metrics

# Или конкретную
python scripts/measure_metrics.py --metric wer
python scripts/measure_metrics.py --metric latency
python scripts/measure_metrics.py --metric offline
```

### 3. `scripts/validate_checklist.py` (улучшен)

**Новые функции:**
- `calculate_file_hash()` — вычисляет SHA256 хеш файла для проверки целостности
- Хеш сохраняется в статистике валидации

**Использование:**
```bash
make audit-checklist
```

### 4. CI/CD интеграция

**Файл:** `.github/workflows/ci.yml`

**Добавлено:**
- Генерация JSON отчёта при запуске тестов
- Автоматическая проверка метрик (dry-run) после тестов

---

## 📊 Поддерживаемые метрики

| Метрика | Epic | Источник | Статус |
|---------|------|----------|--------|
| WER | epic_i_asr | `tests/test_asr_accuracy.py` | ✅ |
| Latency | epic_i_asr | `tests/test_asr_latency.py` | ✅ |
| Офлайн транскрипция | epic_i_asr | `tests/test_asr_offline.py` | ✅ |
| Factual consistency | epic_ii_llm | Тесты summarizer | 🔄 |
| DeepConf score | epic_ii_llm | Тесты critic | 🔄 |
| Token entropy | epic_ii_llm | Тесты summarizer | 🔄 |

---

## 🔄 Workflow

### Локальная разработка

```bash
# 1. Запустить тесты
pytest tests/ -v

# 2. Обновить метрики из отчёта
make update-metrics

# 3. Проверить валидность
make audit-checklist
```

### Перед коммитом

```bash
# 1. Запустить все тесты
make test-all

# 2. Обновить метрики
make update-metrics

# 3. Проверить чеклист
make audit-checklist

# 4. Создать снапшот
python scripts/snapshot_checklist.py
```

### CI/CD

Автоматически:
1. Тесты запускаются с JSON отчётом
2. Метрики проверяются (dry-run)
3. Валидация чеклиста выполняется

---

## 🔒 Безопасность

### Проверка целостности

Валидатор теперь вычисляет SHA256 хеш чеклиста:

```json
{
  "stats": {
    "file_hash": "abc123..."
  }
}
```

Это позволяет:
- Обнаружить ручные правки
- Отследить изменения в истории
- Обеспечить консистентность

### История изменений

`docs/history/` версионируется в Git:
- Снапшоты чеклиста по датам
- JSON отчёты аудита
- История метрик

---

## 📈 Вектор зрелости

| Уровень | Описание | Статус |
|---------|----------|--------|
| **Self-validated** | Чеклист валидируется автоматически | ✅ |
| **Self-measured** | Метрики обновляются из тестов | ✅ |
| **Self-optimized** | Автоматическая оптимизация на основе метрик | 🔄 |

---

## 🎯 Следующие шаги

### Краткосрочные

1. ✅ Добавить поддержку всех метрик из чеклиста
2. ✅ Интегрировать с CI/CD
3. ✅ Добавить проверку целостности (хеш)

### Среднесрочные

1. 🔄 Автоматическое обновление метрик в CI (не только dry-run)
2. 🔄 Визуализация метрик в README (badges)
3. 🔄 Алерты при деградации метрик

### Долгосрочные

1. 🔄 Self-optimized: автоматическая оптимизация на основе метрик
2. 🔄 Predictive metrics: предсказание метрик на основе истории
3. 🔄 Auto-tuning: автоматическая настройка параметров для улучшения метрик

---

## 📝 Примеры

### Обновление WER

```bash
# После теста ASR accuracy
pytest tests/test_asr_accuracy.py -v

# Обновить метрику
make update-metrics

# Результат в чеклисте:
# metrics:
#   - name: "WER"
#     target: "≤ 10%"
#     current: "8.5%"  # ← Автоматически обновлено
```

### Прямое измерение

```bash
# Измерить все метрики напрямую
make measure-metrics

# Результат:
# ✅ Updated epic_i_asr:WER = 8.5%
# ✅ Updated epic_i_asr:Latency = 0.8 сек
# ✅ Updated epic_i_asr:Офлайн транскрипция = ≥ 30 мин
```

---

## 🔍 Отладка

### Метрики не обновляются

1. Проверьте JSON отчёт:
   ```bash
   cat tests/.report.json | jq '.tests[].call.stdout'
   ```

2. Проверьте регулярные выражения в `scripts/auto_measure.py`

3. Запустите в dry-run режиме:
   ```bash
   make update-metrics-dry-run
   ```

### Неправильные значения

1. Убедитесь, что тесты выводят метрики в правильном формате
2. Используйте `scripts/measure_metrics.py` для прямых измерений
3. Проверьте логи тестов

---

## ✅ Итог

Reflexio v2.1 теперь имеет **полноценную систему самопроверки и самопроверки**:

- ✅ Валидация чеклиста
- ✅ Автоматическое измерение метрик
- ✅ Интеграция с CI/CD
- ✅ Проверка целостности
- ✅ История изменений

**Это превращает Reflexio из просто проекта в самоподдерживающуюся систему.**

---

**Подробнее:**
- `docs/AUTO_MEASUREMENT_GUIDE.md` — руководство по использованию
- `scripts/auto_measure.py` — код автоматического обновления
- `scripts/measure_metrics.py` — код прямых измерений





