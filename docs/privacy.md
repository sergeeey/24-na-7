# 🔒 Privacy Policy — Reflexio 24/7

**Дата:** 4 ноября 2025  
**Версия:** 2.1

---

## 🎯 Принципы приватности

Reflexio 24/7 построен на принципах **privacy-first** и **zero-trust**:

1. **Zero-retention** — аудио удаляется после обработки (по умолчанию через 24 часа)
2. **End-to-end шифрование** — все аудио файлы шифруются локально (AES-256)
3. **Row-Level Security (RLS)** — каждый пользователь видит только свои данные
4. **Opt-out training** — пользователь может отказаться от использования данных для обучения
5. **Explainable AI** — прозрачность решений модели

---

## 📊 Что собирается и как используется

### Собираемые данные

1. **Аудио записи**
   - Хранятся локально в зашифрованном виде
   - Удаляются автоматически через 24 часа (zero-retention)
   - Не передаются третьим лицам без явного согласия

2. **Транскрипции**
   - Сохраняются для генерации дайджестов
   - Хранятся с RLS (только владелец имеет доступ)
   - Могут быть удалены пользователем в любой момент

3. **Метаданные**
   - Время записи, длительность, язык
   - Используются для анализа и улучшения сервиса
   - Анонимизируются при аналитике

4. **Предпочтения пользователя**
   - Язык, часовой пояс, настройки уведомлений
   - Хранятся в core memory
   - Могут быть изменены или удалены в любой момент

### Использование данных

- **Генерация дайджестов** — транскрипции используются для создания дневных сводок
- **Улучшение качества** — метаданные используются для оптимизации ASR и LLM
- **Обучение моделей** — только если пользователь не установил `opt_out_training = true`

---

## 🔐 Безопасность данных

### Шифрование

- **Аудио файлы**: AES-256 шифрование локально
- **Транспорт**: HTTPS для всех API запросов
- **База данных**: RLS политики (tenant_id == auth.uid())

### Доступ к данным

- **Пользователь**: только свои данные через RLS
- **Сервис**: только для обработки и генерации дайджестов
- **Третьи лица**: никогда без явного согласия

---

## 🤖 Explainable AI

Reflexio использует **Explainable AI** для прозрачности решений:

### Почему модель сделала вывод?

1. **DeepConf Score** — метрика уверенности модели (0.0-1.0)
   - Если score < 0.85, саммари автоматически улучшается через Refiner
   - Пользователь видит confidence score в дайджесте

2. **Token Entropy** — мера неопределённости модели
   - Низкая entropy (< 0.3) = высокая уверенность
   - Высокая entropy = модель менее уверена

3. **Emotion Analysis** — анализ эмоций в тексте
   - Показывает какие эмоции обнаружены и с какой интенсивностью
   - Используется для контекста в саммари

4. **Chain of Density** — процесс уплотнения саммари
   - Пользователь может видеть итерации уплотнения
   - Понятно, какие детали были добавлены на каждом шаге

### Пример Explainable AI вывода

```json
{
  "summary": "Пользователь обсудил проект с командой...",
  "confidence_score": 0.92,
  "token_entropy": 0.25,
  "explanation": {
    "reasoning": "Высокая уверенность благодаря чётким фактам и низкой энтропии",
    "emotions_detected": ["радость", "заинтересованность"],
    "key_facts": ["обсуждение проекта", "встреча с командой"],
    "refined": false
  }
}
```

---

## 🚫 Opt-out Training

Пользователь может отказаться от использования данных для обучения:

### Как включить opt-out

1. **Через API:**
   ```python
   from src.memory.core_memory import get_core_memory
   core_memory = get_core_memory()
   core_memory.set("opt_out_training", True)
   ```

2. **Через миграцию:**
   ```sql
   UPDATE user_preferences 
   SET opt_out_training = true 
   WHERE user_id = auth.uid();
   ```

3. **Через настройки:**
   - В профиле пользователя установить флаг `opt_out_training = true`

### Что это означает

- Данные **не используются** для обучения моделей
- Данные **не передаются** третьим лицам для ML
- Данные **используются только** для генерации дайджестов пользователя

---

## 🗑️ Zero-Retention Policy

### Аудио файлы

- **По умолчанию**: удаляются через 24 часа после обработки
- **Настройка**: можно изменить через `AUDIO_RETENTION_HOURS` в `.env`
- **Автоматическая очистка**: выполняется ежедневно через retention policy

### Транскрипции

- **По умолчанию**: хранятся 90 дней
- **Настройка**: через `TRANSCRIPTION_RETENTION_DAYS`
- **Удаление**: пользователь может удалить в любой момент

### Дайджесты

- **По умолчанию**: хранятся 365 дней
- **Настройка**: через `DIGEST_RETENTION_DAYS`
- **Экспорт**: пользователь может экспортировать в PDF/JSON

---

## 📝 Права пользователя

Пользователь имеет право:

1. **Доступ** — получить все свои данные в структурированном формате
2. **Исправление** — исправить неточные данные
3. **Удаление** — удалить все свои данные в любой момент
4. **Портативность** — экспортировать данные в JSON/PDF
5. **Отзыв согласия** — отозвать согласие на обработку данных

---

## 🔍 Аудит и логирование

Reflexio ведёт логи для аудита:

- **Доступ к данным**: кто и когда получил доступ
- **Изменения настроек**: изменения privacy настроек
- **Удаления**: когда и какие данные были удалены
- **Ошибки**: ошибки обработки (без PII)

Логи хранятся локально и не содержат персональных данных.

---

## 📞 Контакты

Для вопросов о приватности:

- **Email**: privacy@reflexio.ai (пример)
- **Документация**: `docs/privacy.md`

---

**Последнее обновление:** 4 ноября 2025





