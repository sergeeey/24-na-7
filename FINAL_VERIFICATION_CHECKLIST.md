# 🧭 Финальная проверка готовности — Reflexio 24/7

**DevOps-инженерский чеклист перед production deployment**

---

## 🎯 Цель

Убедиться, что система полностью готова к первому запуску и все компоненты на месте.

---

## 🚀 Быстрая проверка

```bash
# Linux/macOS
chmod +x scripts/final_verification.sh
./scripts/final_verification.sh

# Windows PowerShell
.\scripts\final_verification.ps1
```

---

## 📋 Что проверяется

### 1. Чистота среды

- [ ] Нет запущенных старых контейнеров Reflexio
- [ ] Docker готов к использованию (если нужен)

**Команда:**
```bash
docker ps --filter "name=reflexio"
```

---

### 2. .env файл

- [ ] Файл `.env` существует
- [ ] `DB_BACKEND=supabase`
- [ ] `SUPABASE_URL` заполнен
- [ ] `SUPABASE_ANON_KEY` заполнен
- [ ] `OPENAI_API_KEY` заполнен
- [ ] `SAFE_MODE=strict`

**Команда:**
```bash
cat .env
```

---

### 3. API ключи (два мира)

- [ ] Python `.env` файл настроен
- [ ] MCP ключи в Cursor Settings (для MCP серверов)

**Команда:**
```bash
python scripts/check_api_keys.py
```

---

### 4. Зависимости

- [ ] FFmpeg установлен
- [ ] Python 3.11+ доступен
- [ ] Все Python зависимости установлены

**Команда:**
```bash
ffmpeg -version
python --version
pip list
```

---

### 5. Docker (если нужен)

- [ ] Docker установлен
- [ ] Docker Compose доступен
- [ ] `Dockerfile.api` существует
- [ ] `docker-compose.yml` существует

**Команда:**
```bash
docker --version
docker compose version
```

---

### 6. API endpoints

- [ ] `/health` отвечает (если API запущен)
- [ ] `/metrics/prometheus` доступен

**Команда:**
```bash
curl http://localhost:8000/health
curl http://localhost:8000/metrics/prometheus
```

---

### 7. Supabase

- [ ] Подключение работает
- [ ] Таблицы созданы

**Команда:**
```bash
python -c "from src.storage.db import get_db_backend; db = get_db_backend(); print('OK')"
```

---

### 8. Файлы системы

- [ ] `init.yaml` playbook существует
- [ ] `first_launch.sh/ps1` существует
- [ ] `verify_full_pipeline.py` существует
- [ ] `.cursor/mcp.json` существует

---

## ✅ Критерии успеха

После выполнения `final_verification.sh`:

- ✅ Все обязательные проверки пройдены
- ⚠️ Предупреждения допустимы (опциональные компоненты)
- ❌ Нет критических ошибок

---

## 🚀 После проверки

Если все проверки пройдены:

```bash
# Запуск системы
./scripts/first_launch.sh

# Или вручную
@playbook init-reflexio
docker compose up -d
```

---

## 🔍 Детальная проверка Supabase

Если нужно проверить Supabase вручную:

1. Откройте Supabase Dashboard → SQL Editor
2. Выполните:
   ```sql
   SELECT COUNT(*) FROM missions;
   SELECT COUNT(*) FROM metrics;
   ```
3. Если таблицы пусты — это нормально для первого запуска
4. Если ошибок нет — подключение работает

---

## 📊 Отчёты

Скрипт проверки создаёт отчёты в `.cursor/audit/`:

- `api_keys_check.json`
- `full_pipeline_verification.json`
- `prod_readiness_report.json`

---

**Последнее обновление:** 3 ноября 2025  
**Статус:** ✅ Ready for Production











