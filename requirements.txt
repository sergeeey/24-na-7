# Requirements для Reflexio 24/7 v2.1
# Сгенерировано из pyproject.toml

# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
structlog>=23.2.0

# Audio Processing
webrtcvad>=2.0.10
sounddevice>=0.4.6
numpy>=1.26.0
librosa>=0.10.0

# ASR
faster-whisper>=0.10.0
ctranslate2>=3.20.0
openai>=1.0.0

# LLM
anthropic>=0.18.0

# HTTP & Requests
requests>=2.31.0

# Rate Limiting
slowapi>=0.1.9
redis>=5.0.0  # Для хранения rate limit счетчиков

# Secrets Management (P0-3)
hvac>=2.0.0  # HashiCorp Vault client

# Input Validation & Guardrails (P0-4)
pydantic>=2.0.0  # Уже есть, но явно укажем

# Database
psycopg2-binary>=2.9.0
supabase>=2.0.0

# Telegram
python-telegram-bot>=20.0

# PDF Generation
reportlab>=4.0.0

# Encryption
cryptography>=41.0.0

# Embeddings
sentence-transformers>=2.2.0

# Speaker Verification (voice diarization)
# ПОЧЕМУ resemblyzer: GE2E LSTM 18MB, pip install без HuggingFace gating,
# ~50ms на сегмент CPU. Достаточно для binary "is_user?" verification.
resemblyzer>=0.1.1

# Stripe (опционально)
stripe>=7.0.0

# Letta SDK (опционально)
# letta>=0.1.0  # Раскомментировать если используется

# pyAudioAnalysis (опционально, для эмоционального анализа)
# pyAudioAnalysis>=0.3.0  # Раскомментировать если используется





