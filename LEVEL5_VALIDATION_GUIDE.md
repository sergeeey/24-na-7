# Level 5 Validation Guide

**Руководство по проверке компонентов Reflexio 24/7 Level 5 (Self-Adaptive)**

---

## 🚀 Быстрая проверка

### Запуск автоматической валидации

```bash
@playbook validate-level5
```

или

```bash
python .cursor/validation/level5_validation.py
```

---

## 📋 Проверяемые компоненты

### 1. DeepConf Feedback Loop

**Проверки:**
- ✅ `cursor-metrics.json` существует
- ✅ `avg_deepconf_confidence` присутствует и валиден (0.0-1.0)
- ✅ Скрипт `deepconf_feedback.py` доступен

**Если проверка провалена:**
1. Убедитесь, что выполнена хотя бы одна OSINT миссия
2. Проверьте наличие `cursor-metrics.json` в корне проекта
3. Если файла нет, создайте его с базовой структурой:

```json
{
  "metrics": {
    "osint": {
      "avg_deepconf_confidence": 0.85,
      "missions_completed": 0,
      "total_claims": 0,
      "validated_claims": 0
    }
  }
}
```

---

### 2. Adaptive Mission Scoring

**Проверки:**
- ✅ Скрипт `adaptive_scoring.py` доступен
- ✅ Директория результатов существует
- ✅ Формула scoring работает корректно

**Если проверка провалена:**
1. Убедитесь, что модуль `src/osint/adaptive_scoring.py` существует
2. Запустите хотя бы одну OSINT миссию для создания результатов
3. Проверьте наличие `.cursor/osint/results/`

---

### 3. Memory Curation Agent

**Проверки:**
- ✅ Скрипт `memory_curator.py` доступен
- ✅ Memory Bank существует (или будет создан автоматически)
- ✅ Возможность удаления опровергнутых утверждений

**Если проверка провалена:**
1. Проверьте наличие `src/osint/memory_curator.py`
2. При первом запуске Memory Bank будет создан автоматически
3. Если есть опровергнутые утверждения, запустите:

```bash
python -m src.osint.memory_curator --remove-refuted
```

---

### 4. Governance Integration

**Проверки:**
- ✅ `profile.yaml` существует
- ✅ Секция `osint_governance` присутствует
- ✅ `governance_loop.py` интегрирован с OSINT
- ✅ Текущий уровень ≥ 4

**Если проверка провалена:**
1. Запустите апгрейд:

```bash
@playbook level5-self-adaptive-upgrade
```

2. Проверьте `.cursor/governance/profile.yaml` на наличие:
   ```yaml
   osint_governance:
     avg_deepconf_confidence: null
     missions_completed: 0
     knowledge_health: "unknown"
     auto_regeneration_active: false
   ```

3. Если секция отсутствует, добавьте её вручную или перезапустите апгрейд

---

### 5. MCP Intelligence Pack

**Проверки:**
- ✅ `.cursor/mcp.json` существует
- ✅ Brave Search настроен и включён
- ✅ Bright Data настроен и включён
- ✅ API ключи присутствуют в `.env` (предупреждение)
- ✅ MCP клиенты импортируются

**Если проверка провалена:**
1. Проверьте `.cursor/mcp.json`:
   ```json
   {
     "mcpServers": {
       "brave": {
         "enabled": true,
         "api_key_env": "BRAVE_API_KEY"
       },
       "brightdata": {
         "enabled": true,
         "api_key_env": "BRIGHTDATA_API_KEY"
       }
     }
   }
   ```

2. Добавьте ключи в `.env`:
   ```bash
   BRAVE_API_KEY=your_key_here
   BRIGHTDATA_API_KEY=your_key_here
   ```

3. Проверьте доступность модулей:
   ```bash
   python -c "from src.mcp.clients import get_brave_client, get_bright_client"
   ```

---

### 6. Playbooks & Hooks

**Проверки:**
- ✅ Playbooks существуют (`osint-mission.yaml`, `level5-upgrade.yaml`, `validate-mcp.yaml`)
- ✅ Hook для `new_topic_detected` присутствует
- ✅ Hook интегрирован с intelligence

**Если проверка провалена:**
1. Убедитесь, что все playbooks находятся в `.cursor/playbooks/`
2. Проверьте `.cursor/hooks/on_event.py` на наличие:
   ```python
   def on_new_topic_detected(topic: str):
       from src.mcp.intelligence import combined_search_and_scrape, save_to_memory_bank
       # ...
   ```

---

## 🔧 Исправление типичных проблем

### Проблема: `cursor-metrics.json` не найден

**Решение:**
1. Создайте файл вручную (см. выше)
2. Или запустите хотя бы одну миссию/операцию, которая создаст метрики

### Проблема: Секция `osint_governance` отсутствует

**Решение:**
```bash
@playbook level5-self-adaptive-upgrade
```

или добавьте вручную в `.cursor/governance/profile.yaml`

### Проблема: API ключи не настроены

**Решение:**
1. Получите API ключи от Brave Search и Bright Data
2. Добавьте в `.env`:
   ```bash
   BRAVE_API_KEY=your_key
   BRIGHTDATA_API_KEY=your_key
   ```
3. Перезапустите валидацию

### Проблема: Нет результатов миссий

**Решение:**
1. Запустите тестовую миссию:
   ```bash
   @playbook osint-mission --mission_file .cursor/osint/missions/example_mission.json
   ```

---

## 📊 Интерпретация результатов

### Все проверки пройдены ✅

**Результат:** Reflexio 24/7 готов к использованию на Level 5!

### Есть предупреждения ⚠️

**Действие:** Проверьте предупреждения, но система может работать.

**Типичные предупреждения:**
- API ключи не настроены (функциональность будет ограничена)
- Memory Bank ещё не создан (будет создан автоматически)
- Нет результатов миссий (запустите хотя бы одну)

### Есть проваленные проверки ❌

**Действие:** Исправьте ошибки перед использованием.

**Приоритет:**
1. Критичные (отсутствие скриптов, неправильная интеграция)
2. Важные (отсутствие конфигурации)
3. Желательные (API ключи, результаты миссий)

---

## 🔄 Регулярная проверка

Рекомендуется запускать валидацию:
- После каждого апгрейда
- Перед важными миссиями
- При появлении ошибок
- Еженедельно для мониторинга состояния

---

## 📄 Отчёт валидации

После каждой проверки создаётся отчёт:
`.cursor/validation/level5_validation_report.json`

Содержит:
- Детали всех проверок
- Статус каждой проверки
- Итоговую сводку
- Предупреждения и ошибки

---

## ✅ Критерии успеха

**Минимальные требования для Level 5:**
- ✅ Все скрипты доступны (0 провалов)
- ✅ Governance интегрирован
- ✅ MCP настроен
- ✅ Playbooks и hooks на месте

**Оптимальные условия:**
- ✅ API ключи настроены
- ✅ Выполнена хотя бы одна миссия
- ✅ Memory Bank создан
- ✅ Метрики актуальны

---

**После успешной валидации Reflexio 24/7 готов к автономной работе на Level 5!** 🎯✨













