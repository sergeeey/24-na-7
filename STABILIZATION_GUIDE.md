# 📘 Руководство по стабилизации Reflexio 24/7

**Трёхдневный план для перехода от Automated к Self-Adaptive**

---

## 🎯 Цель стабилизации

Переход Reflexio 24/7 от уровня "работающего проекта" к **самоорганизующейся когнитивной системе**, способной функционировать автономно и адаптироваться к изменениям.

---

## 📅 Трёхдневный план

### День 1: Поведенческая калибровка

**Что происходит:**
- Система запускается с чистыми логами
- Агенты работают автономно
- Сбор метрик утром и вечером

**Запуск:**
```bash
@playbook stabilization-reflexio --day 1
```

**Проверка:**
```bash
# Утром - начальные метрики
python scripts/stabilization/collect_daily_state.py --day 1 --period morning

# Вечером - финальные метрики
python scripts/stabilization/collect_daily_state.py --day 1 --period evening

# Отчёт
cat .cursor/stabilization/day1/daily_report.md
```

**Критерий успеха:** 12+ часов стабильной работы без вмешательства.

---

### День 2: Адаптивная реакция

**Что происходит:**
- Стресс-тест: искусственное нарушение MCP-сервиса
- Проверка реакции Governance Loop
- Восстановление и проверка самовосстановления

**Запуск:**
```bash
@playbook stabilization-reflexio --day 2
```

**Стресс-тест:**
1. Изменить `SUPABASE_URL` в `.env` на неверное значение
2. Подождать 10 минут
3. Проверить Governance:
   ```bash
   cat .cursor/governance/profile.yaml | grep -A 5 "mcp_governance"
   ```
4. Восстановить правильный `SUPABASE_URL`
5. Проверить восстановление:
   ```bash
   @playbook validate-mcp
   ```

**Критерий успеха:** Система обнаруживает деградацию, логирует и восстанавливается автоматически.

---

### День 3: Когнитивная когерентность

**Что происходит:**
- Полный когнитивный цикл (дайджест → аудит → метрики)
- Проверка согласованности всех процессов
- Финальная оценка готовности

**Запуск:**
```bash
@playbook stabilization-reflexio --day 3
```

**Финальная проверка:**
```bash
# Полный аудит
@playbook audit-standard

# Проверка готовности
cat .cursor/audit/audit_report.json | grep score
```

**Критерий успеха:** 
- Балл аудита ≥ 85
- Все компоненты работают согласованно
- Нет критических проблем

---

## 📊 Метрики для отслеживания

### Ежедневно проверяйте:

| Метрика | Где смотреть | Порог тревоги |
|---------|-------------|---------------|
| MCP latency | `.cursor/metrics/mcp_health.json` | > 2000 ms |
| Governance profile | `.cursor/governance/profile.yaml` | Частые переключения |
| API errors | `/metrics` endpoint | > 1% |
| Pending queue | Database `ingest_queue` | > 10 items |
| Reliability | Governance profile | < 0.75 |

### Сбор метрик:

```bash
# Утренние метрики
python scripts/stabilization/collect_daily_state.py --day X --period morning

# Вечерние метрики
python scripts/stabilization/collect_daily_state.py --day X --period evening

# Проверка здоровья
python scripts/stabilization/check_health.py --day X
```

---

## 📈 График динамики

После каждого дня автоматически строится график:

```bash
# Просмотр timeline
cat .cursor/stabilization/reliability_timeline.json

# График (если matplotlib установлен)
# Файл: .cursor/stabilization/reliability_graph.png
```

График показывает:
- Динамику AI Reliability Index
- Состояние MCP-сервисов
- Изменения профилей Governance

---

## ✅ Критерии готовности к Level 5

После трёх дней стабилизации проверьте:

| Критерий | Ожидаемое значение |
|----------|-------------------|
| **Аудит балл** | ≥ 85 / 100 |
| **AI Reliability** | ≥ 0.85 |
| **Context Hit Rate** | ≥ 0.75 |
| **MCP Services** | Все healthy |
| **Validation** | SAFE+CoVe пройдена |
| **Governance** | Стабильный профиль |
| **Digest** | Когнитивные метрики включены |

---

## 🚨 Что делать при проблемах

### Проблема: MCP сервисы недоступны

```bash
# Проверить конфигурацию
cat .cursor/mcp.json

# Проверить health
python scripts/check_mcp_health.py --summary

# Перезапустить валидацию
@playbook validate-mcp
```

### Проблема: Governance часто переключается

```bash
# Проверить профиль
cat .cursor/governance/profile.yaml

# Проверить политики
grep -A 3 "governance_policies" .cursor/governance/profile.yaml

# Запустить аудит
@playbook audit-standard
```

### Проблема: Низкий Reliability Index

```bash
# Проверить компоненты
python .cursor/audit/run_audit.py --mode standard

# Проверить метрики
cat cursor-metrics.json

# Запустить сбор метрик
python scripts/metrics_snapshot.py
```

---

## 📁 Структура данных стабилизации

```
.cursor/stabilization/
├── day1/
│   ├── morning_metrics.json      # Утренние метрики
│   ├── evening_metrics.json      # Вечерние метрики
│   ├── health_check.json         # Проверка здоровья
│   ├── mcp_health_morning.json   # MCP health утром
│   ├── mcp_health_evening.json   # MCP health вечером
│   ├── daily_report.md           # Дневной отчёт
│   └── session.log               # Лог сессии
├── day2/ (аналогично)
├── day3/ (аналогично)
├── reliability_timeline.json     # Timeline всех дней
└── reliability_graph.png         # График (опционально)
```

---

## 🎓 Рекомендации

1. **Не вмешивайтесь** без необходимости в течение дня
2. **Логируйте** все ручные действия в `session.log`
3. **Сравнивайте** утренние и вечерние метрики
4. **Отслеживайте** тренды на графике
5. **Документируйте** любые неожиданные поведения

---

## 🚀 После стабилизации

Если все критерии выполнены:

1. Запустите финальный аудит:
   ```bash
   @playbook audit-standard
   ```

2. Проверьте балл (должен быть ≥ 85)

3. Если готово → **Reflexio 24/7 готов к Level 5 — Self-Adaptive!** 🎉

---

*Этот процесс превращает Reflexio из "работающего проекта" в самоорганизующуюся когнитивную систему.*













