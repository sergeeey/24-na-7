# MCP Configuration Validation Guide

**Автоматическая валидация конфигурации MCP-серверов**

---

## 🎯 Возможности

### Проверка структуры
- ✅ Валидность JSON
- ✅ Наличие обязательных секций
- ✅ Корректность типов данных

### Проверка серверов
- ✅ Наличие `command` или `url` для каждого сервера
- ✅ Корректность URL (http/https/ws/wss)
- ✅ Доступность команд в системе
- ✅ Наличие переменных окружения

### Проверка окружения
- ✅ Наличие API ключей в `.env`
- ✅ Корректность ссылок на переменные
- ✅ Соответствие env-переменных требованиям

---

## 🚀 Быстрый запуск

### Через Playbook

```bash
@playbook validate-mcp-config
```

### Напрямую через Python

```bash
python scripts/validate_mcp_config.py
```

### С сохранением отчёта

```bash
python scripts/validate_mcp_config.py \
  --output-json .cursor/audit/mcp_config_validation.json \
  --output-markdown .cursor/audit/mcp_config_validation.md
```

---

## 📊 Что проверяется

### 1. Структура конфигурации

| Проверка | Описание |
|----------|----------|
| JSON валидность | Корректность синтаксиса JSON |
| Обязательные секции | Наличие `mcpServers` |
| Версия | Наличие поля `version` |

### 2. Конфигурация серверов

| Проверка | Описание |
|----------|----------|
| Способ запуска | Наличие `command` или `url` |
| Корректность URL | Начинается с http/https/ws/wss |
| Доступность команд | Команда найдена в PATH |
| Args для command | Наличие аргументов |
| Enabled флаг | Явное указание состояния |

### 3. Переменные окружения

| Проверка | Описание |
|----------|----------|
| API ключи | Наличие `api_key_env` в системе |
| Env ссылки | Корректность `${VAR}` ссылок |
| Доступность | Переменные читаются из `.env` |

### 4. Дополнительные проверки

| Проверка | Описание |
|----------|----------|
| Capabilities | Корректность списка возможностей |
| Services | Структура секции services |
| Connectivity | Структура секции connectivity |
| Metadata | Валидность метаданных (level 1-5) |

---

## 📄 Отчёты

### JSON отчёт

`.cursor/audit/mcp_config_validation.json`

```json
{
  "timestamp": "2025-11-03T21:00:00Z",
  "config_path": ".cursor/mcp.json",
  "valid": true,
  "summary": {
    "errors": 0,
    "warnings": 2,
    "info": 1
  },
  "errors": [],
  "warnings": [
    {
      "component": "server.brave",
      "message": "Переменная окружения 'BRAVE_API_KEY' не установлена",
      "severity": "warning"
    }
  ],
  "info": [
    {
      "component": "mcpServers",
      "message": "Найдено серверов: 4, включено: 3",
      "severity": "info"
    }
  ]
}
```

### Markdown отчёт

`.cursor/audit/mcp_config_validation.md`

Человекочитаемый отчёт с:
- Кратким резюме
- Детальным списком ошибок
- Предупреждениями
- Информационными сообщениями
- Рекомендациями

---

## 🔍 Примеры использования

### Проверка конкретного файла

```bash
python scripts/validate_mcp_config.py \
  --config .cursor/mcp.custom.json
```

### С прерыванием при ошибках

```bash
python scripts/validate_mcp_config.py \
  --fail-on-errors
```

Полезно для CI/CD пайплайнов.

### Интеграция с другими скриптами

```python
from scripts.validate_mcp_config import MCPConfigValidator

validator = MCPConfigValidator(Path(".cursor/mcp.json"))
results = validator.validate_all()

if not results["valid"]:
    print("Configuration has errors!")
    for error in results["errors"]:
        print(f"  - {error['message']}")
```

---

## ⚠️ Типичные ошибки и решения

### Ошибка: "Сервер должен иметь либо 'command', либо 'url'"

**Проблема:** Сервер не имеет способа запуска.

**Решение:** Добавьте либо `command` (для локальных серверов), либо `url` (для внешних API).

```json
{
  "brave": {
    "url": "https://api.search.brave.com/res/v1/web/search",
    "enabled": true
  }
}
```

### Предупреждение: "Переменная окружения 'BRAVE_API_KEY' не установлена"

**Проблема:** API ключ не найден в окружении.

**Решение:**
1. Добавьте ключ в `.env`:
   ```bash
   BRAVE_API_KEY=ваш_ключ
   ```
2. Убедитесь, что `.env` загружается перед запуском.

### Ошибка: "Некорректный URL: api.brightdata.com"

**Проблема:** URL не содержит протокол.

**Решение:** Добавьте протокол:
```json
{
  "brightdata": {
    "url": "https://api.brightdata.com",
    "enabled": true
  }
}
```

### Предупреждение: "Команда 'uvicorn' не найдена в PATH"

**Проблема:** Команда недоступна в системе.

**Решение:**
1. Установите зависимость:
   ```bash
   pip install uvicorn
   ```
2. Или используйте полный путь:
   ```json
   {
     "command": "/usr/local/bin/uvicorn",
     "args": [...]
   }
   ```

---

## 🔄 Автоматическая валидация

### В Hooks System

Добавьте в `.cursor/hooks/hooks.json`:

```json
{
  "on_mcp_config_change": {
    "enabled": true,
    "trigger": ".cursor/mcp.json",
    "action": "@playbook validate-mcp-config",
    "description": "Валидация конфигурации при изменении mcp.json"
  }
}
```

### В Governance Loop

Добавьте проверку конфигурации в аудит:

```yaml
# .cursor/playbooks/audit.yaml
steps:
  - name: Validate MCP Config
    run: "@playbook validate-mcp-config"
```

### В CI/CD

```yaml
# .github/workflows/validate.yml
- name: Validate MCP Config
  run: |
    python scripts/validate_mcp_config.py --fail-on-errors
```

---

## 📈 Метрики

После валидации метрики автоматически обновляются в `cursor-metrics.json`:

```json
{
  "metrics": {
    "mcp": {
      "config_valid": true,
      "config_errors": 0,
      "config_warnings": 2,
      "config_last_validation": "2025-11-03T21:00:00Z"
    }
  }
}
```

---

## ✅ Рекомендации

### Еженедельная валидация

Запускайте валидацию раз в неделю:

```bash
@playbook validate-mcp-config
```

### Перед деплоем

Перед развёртыванием проверьте конфигурацию:

```bash
python scripts/validate_mcp_config.py --fail-on-errors
```

### При изменении конфигурации

После изменения `.cursor/mcp.json` всегда запускайте валидацию:

```bash
@playbook validate-mcp-config
```

---

## 🔧 Расширение валидатора

### Добавление собственных проверок

```python
from scripts.validate_mcp_config import MCPConfigValidator

class CustomValidator(MCPConfigValidator):
    def validate_custom_rule(self):
        # Ваша логика проверки
        if some_condition:
            self.errors.append({
                "component": "custom",
                "message": "Custom validation failed",
                "severity": "error",
            })
```

---

**MCP Configuration Validator готов к использованию!** 🎯✨

После каждой проверки вы будете получать детальный отчёт о состоянии конфигурации MCP и сможете быстро исправлять проблемы.












