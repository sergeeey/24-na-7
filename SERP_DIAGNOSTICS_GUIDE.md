# SERP Diagnostics Guide

**Автоматическая диагностика поисковых систем и зон Bright Data**

---

## 🎯 Возможности

### 1. Проверка всех поисковых систем
- ✅ Google Search
- ✅ Bing Search
- ✅ Yahoo Search (если настроено)

### 2. Тестирование всех зон
- Проверка доступности каждой зоны
- Измерение задержки на каждой зоне
- Подсчёт количества результатов

### 3. Автоматический рейтинг
- 🚀 **По латентности** — самые быстрые зоны
- ✅ **По успешности** — самые надёжные зоны
- 📊 **По результатам** — самые продуктивные зоны

---

## 🚀 Быстрый запуск

### Через Playbook

```bash
@playbook serp-diagnostics
```

### Напрямую через Python

```bash
python scripts/serp_diagnostics.py
```

---

## 📊 Что проверяется

### Для каждой зоны:
1. **Доступность** — работает ли зона
2. **Латентность** — средняя задержка запросов
3. **Успешность** — процент успешных запросов
4. **Продуктивность** — количество полученных результатов

### Тестовые запросы:
- "artificial intelligence"
- "latest technology news"
- "climate change"

---

## 📄 Отчёты

### JSON отчёт
`.cursor/audit/serp_diagnostics.json`

Структурированные данные для автоматической обработки:
```json
{
  "timestamp": "2025-11-03T21:00:00Z",
  "zones": {
    "serp_api1": {
      "status": "healthy",
      "summary": {
        "success_rate": 95.5,
        "avg_latency_ms": 234.5,
        "total_results": 30
      }
    }
  },
  "rankings": {
    "by_latency": [...],
    "by_success_rate": [...],
    "by_results_count": [...]
  }
}
```

### Markdown отчёт
`.cursor/audit/serp_diagnostics.md`

Человекочитаемый отчёт с:
- Кратким резюме
- Рейтингами зон
- Детальными результатами для каждой зоны
- Рекомендациями по использованию

---

## 🏆 Рейтинги

### 1. Fastest Zones (по латентности)

Зоны с наименьшей задержкой:

| Rank | Zone | Latency (ms) | Success Rate |
|------|------|--------------|--------------|
| 1 | `serp_api1` | 234ms | 95.5% |
| 2 | `serp_api2` | 267ms | 93.2% |

### 2. Most Reliable Zones (по успешности)

Зоны с наивысшим процентом успешных запросов:

| Rank | Zone | Success Rate | Latency (ms) |
|------|------|--------------|--------------|
| 1 | `serp_api1` | 95.5% | 234ms |
| 2 | `serp_api2` | 93.2% | 267ms |

### 3. Most Productive Zones (по результатам)

Зоны, возвращающие больше всего результатов:

| Rank | Zone | Results Count | Success Rate |
|------|------|--------------|--------------|
| 1 | `serp_api1` | 30 | 95.5% |
| 2 | `serp_api2` | 28 | 93.2% |

---

## 📈 Интерпретация результатов

### Статусы зон

| Статус | Условие | Значение |
|--------|---------|----------|
| **healthy** | Success rate ≥ 90% | ✅ Зона работает отлично |
| **degraded** | 50% ≤ Success rate < 90% | ⚠️ Зона работает, но есть проблемы |
| **unhealthy** | Success rate < 50% | ❌ Зона не работает нормально |

### Метрики качества

**Latency:**
- < 300ms — отлично
- 300-500ms — хорошо
- 500-1000ms — приемлемо
- > 1000ms — медленно

**Success Rate:**
- ≥ 95% — отлично
- 90-95% — хорошо
- 80-90% — приемлемо
- < 80% — проблемы

**Results Count:**
- ≥ 20 — отлично
- 10-20 — хорошо
- 5-10 — мало
- < 5 — очень мало

---

## 🔄 Автоматическое использование

### В миссиях

Reflexio автоматически выбирает лучшую зону на основе рейтинга:

```python
from src.osint.zone_manager import get_zone_for_mission
from scripts.serp_diagnostics import SERPDiagnostics

# Загружаем последний рейтинг
diagnostics = SERPDiagnostics()
results = diagnostics.run_full_diagnostics()

# Используем лучшую зону
best_zone = results["rankings"]["by_latency"][0]["zone"]
```

### Интеграция с Monitoring Agent

Добавьте в расписание мониторинга:

```yaml
# .cursor/playbooks/daily-monitoring.yaml
- name: SERP Health Check
  run: "@playbook serp-diagnostics"
  schedule: "0 9 * * *"  # Каждый день в 9:00
```

---

## 🧪 Тестирование вручную

### Тест конкретной зоны

```python
from scripts.serp_diagnostics import SERPDiagnostics

diagnostics = SERPDiagnostics()

# Тестируем зону
result = diagnostics.test_zone(
    zone_name="serp_api1",
    zone_config={"type": "serp", "engines": ["google", "bing"]},
)

print(result)
```

### Тест поисковой системы

```python
from scripts.serp_diagnostics import SERPDiagnostics

diagnostics = SERPDiagnostics()

# Тестируем Google через зону
result = diagnostics.test_serp_engine(
    search_engine="google",
    zone="serp_api1",
    query="artificial intelligence",
)

print(result)
```

---

## 📊 Метрики в cursor-metrics.json

После диагностики метрики автоматически обновляются:

```json
{
  "metrics": {
    "osint": {
      "serp_zones_tested": 2,
      "serp_zones_healthy": 2,
      "serp_best_zone": "serp_api1",
      "serp_best_latency_ms": 234.5,
      "serp_last_diagnostics": "2025-11-03T21:00:00Z"
    }
  }
}
```

---

## 🔧 Решение проблем

### Ошибка: "API key required for SERP"

**Решение:** Убедитесь, что `BRIGHTDATA_API_KEY` установлен в `.env`

### Ошибка: "No zones configured"

**Решение:**
1. Создайте `.cursor/config/brightdata_zones.json`
2. Добавьте хотя бы одну SERP зону
3. Запустите диагностику снова

### Все зоны unhealthy

**Решение:**
1. Проверьте доступность Bright Data API
2. Проверьте правильность Zone ID в конфигурации
3. Проверьте баланс и лимиты в панели Bright Data
4. Убедитесь, что зоны настроены для SERP API

### Высокая латентность

**Решение:**
1. Проверьте нагрузку в панели Bright Data
2. Попробуйте другую зону
3. Проверьте сетевое подключение
4. Используйте зону в том же регионе, что и сервер

---

## ✅ Рекомендации

### Еженедельная диагностика

Запускайте диагностику раз в неделю для мониторинга здоровья:

```bash
@playbook serp-diagnostics
```

### Перед важными миссиями

Перед запуском важных OSINT миссий проверьте состояние зон:

```bash
python scripts/serp_diagnostics.py
```

### Автоматический выбор зоны

Reflexio может автоматически использовать лучшую зону из рейтинга:

```python
from src.osint.zone_manager import get_zone_manager

manager = get_zone_manager()
# Система использует последний рейтинг для выбора зоны
```

---

**SERP Diagnostics готов к использованию!** 🎯✨













