# Reflexio 24/7 — Быстрый старт

Пошаговая инструкция для запуска "умного диктофона" **прямо сейчас**.

---

## 💻 Вариант 1 — Локальный запуск на компьютере

### Шаг 1: Проверка окружения

```bash
# Проверить Python (нужен ≥ 3.10)
python --version

# Проверить FFmpeg
ffmpeg -version
```

Если FFmpeg не установлен:
- **macOS**: `brew install ffmpeg`
- **Linux**: `sudo apt install ffmpeg` или `sudo yum install ffmpeg`
- **Windows**: [Скачать с ffmpeg.org](https://ffmpeg.org/download.html) и добавить в PATH

### Шаг 2: Установка зависимостей

В корне проекта:

```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv

# Активировать
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установить зависимости
pip install -e ".[dev]"
```

### Шаг 3: Настройка .env

```bash
# Скопировать пример
cp .env.example .env

# При необходимости отредактировать
# Редактор по умолчанию:
# Linux/macOS: nano .env
# Windows: notepad .env
```

Основные настройки в `.env`:
```env
API_URL=http://127.0.0.1:8000
EDGE_AUTO_UPLOAD=true
EDGE_DELETE_AFTER_UPLOAD=true
AUTO_TRANSCRIBE=true  # Автоматическая транскрипция после загрузки
```

### Шаг 4: Запуск API сервера

**Терминал 1**:

```bash
uvicorn src.api.main:app --host 127.0.0.1 --port 8000 --reload
```

Проверка:

```bash
curl http://127.0.0.1:8000/health
```

Ожидаемый ответ:
```json
{"status": "ok", "timestamp": "...", "version": "0.1.0"}
```

### Шаг 5: Запуск слушателя (диктофона)

**Терминал 2**:

```bash
python src/edge/listener.py http://127.0.0.1:8000
```

Что происходит:
- ✅ Микрофон слушает в реальном времени (16 kHz, моно)
- ✅ **webrtcvad** вырезает тишину (порог 2 секунды)
- ✅ Сохраняет сегменты в `src/storage/recordings/`
- ✅ Автоматически отправляет на `/ingest/audio`
- ✅ Удаляет локальный файл после успешной отправки (Zero-retention)

**Проверка**: Скажи короткую фразу. В логах должно появиться:

```
[edge] Speech segment saved: recordings/20250103_211722.wav
[edge] Audio uploaded: server_id=abc123, size=45678
```

### Шаг 6: Проверка транскрипции

Если `AUTO_TRANSCRIBE=true` в `.env`, транскрипция запустится автоматически.

Иначе вручную:

```bash
# Сначала получить file_id из ответа API или найти в логах
python scripts/trigger_transcription.py --url http://127.0.0.1:8000 --in sample_file_id.txt
```

---

## 📱 Вариант 2 — Android (Edge устройство)

### Вариант 2A: Использование готовых приложений

#### Что нужно

1. **Smart Recorder** (Google Play)
   - Настройки:
     - Формат: `.wav`
     - «Пропуск тишины» — включено
     - Частота: 16 kHz
     - Каталог: `/storage/emulated/0/Recordings/Reflexio/`

2. **FolderSync** (Google Play)
   - Подключить Google Drive / SFTP / WebDAV
   - Правило синхронизации:
     - Исходная папка: `Recordings/Reflexio/`
     - Цель: твой сервер `/storage/uploads/` (через SFTP/WebDAV)
     - Условия: только Wi-Fi, на зарядке
     - Автосинхронизация: каждые 10 минут

#### Процесс

1. Включи Smart Recorder → записывает только речь
2. FolderSync доставляет файлы на сервер
3. Reflexio API автоматически обрабатывает (если настроен файловый мониторинг)

---

### Вариант 2B: Termux (продвинутый)

1. Установи **Termux** из F-Droid

2. Обнови пакеты:
   ```bash
   pkg update && pkg upgrade
   pkg install python git
   ```

3. Установи зависимости:
   ```bash
   pip install sounddevice webrtcvad requests numpy
   ```

4. Скопируй проект на устройство (через git или USB)

5. Настрой `.env`:
   ```bash
   nano .env
   ```
   Укажи URL твоего сервера:
   ```env
   API_URL=https://your-server.com
   ```

6. Запусти listener:
   ```bash
   python src/edge/listener.py
   ```

7. (Опционально) Запуск в фоне:
   ```bash
   nohup python src/edge/listener.py > listener.log 2>&1 &
   ```

---

## 🚀 Запуск в фоновом режиме (24/7)

### Linux/macOS

#### Вариант 1: nohup

```bash
nohup python src/edge/listener.py http://127.0.0.1:8000 > listener.log 2>&1 &
```

#### Вариант 2: systemd (Linux)

Создай файл `/etc/systemd/system/reflexio-listener.service`:

```ini
[Unit]
Description=Reflexio 24/7 Listener
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/reflexio
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python src/edge/listener.py http://127.0.0.1:8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable reflexio-listener
sudo systemctl start reflexio-listener
sudo systemctl status reflexio-listener
```

### Windows

#### Вариант 1: pythonw.exe

```cmd
pythonw.exe src\edge\listener.py http://127.0.0.1:8000
```

#### Вариант 2: NSSM (Non-Sucking Service Manager)

1. Скачай [NSSM](https://nssm.cc/download)
2. Установи как службу:

```cmd
nssm install ReflexioListener
```

В GUI укажи:
- Path: `C:\path\to\venv\Scripts\python.exe`
- Arguments: `src\edge\listener.py http://127.0.0.1:8000`
- Working Directory: `C:\path\to\reflexio`

Запусти:

```cmd
nssm start ReflexioListener
```

---

## 🧪 Эксперимент: "Reflexio Loop Test"

Проверка полного цикла:

1. **Скажи фразу**:
   > "Сделать заметку: проверить микрофон завтра"

2. **Listener** сохраняет сегмент → отправляет на API

3. **API** принимает → сохраняет в `storage/uploads/`

4. **ASR** транскрибирует → текст в базе

5. **Проверка транскрипции**:
   ```bash
   curl http://127.0.0.1:8000/asr/transcribe?file_id=YOUR_FILE_ID
   ```

6. **Дайджест** (если настроен):
   ```bash
   curl http://127.0.0.1:8000/digest/today
   ```

---

## 🔧 Автономный режим (listener_standalone.py)

Если нужно запустить listener **без установки всего проекта**, используй:

```bash
python listener_standalone.py
```

Этот скрипт:
- ✅ Сам создаёт `.env` при первом запуске
- ✅ Работает независимо от структуры проекта
- ✅ Все зависимости в одном файле
- ✅ Автоматическая отправка на сервер
- ✅ Логирование в консоль и файл

Подробнее см. `listener_standalone.py`.

---

## 🎯 Фильтрация музыки и шума

Reflexio может автоматически отсекать музыку, телевизор и фоновый шум, оставляя только речь.

### Включение фильтра

В `.env` добавьте:

```bash
FILTER_MUSIC=true
FILTER_METHOD=energy
```

Фильтр анализирует спектр аудио и определяет, является ли сигнал человеческой речью. Подробнее: [FILTERS.md](FILTERS.md)

---

## 🐛 Устранение проблем

### Проблема: "No module named 'webrtcvad'"

**Решение**:
```bash
pip install webrtcvad
```

### Проблема: "No audio input device found"

**Решение**:
- Проверь, что микрофон подключен и разрешён в системе
- Linux: проверь `pulseaudio` или `alsa`
- Windows: проверь настройки приватности → Микрофон

### Проблема: "Connection refused" при отправке на сервер

**Решение**:
- Убедись, что API сервер запущен: `curl http://127.0.0.1:8000/health`
- Проверь `API_URL` в `.env`
- Проверь firewall/антивирус

### Проблема: "No module named 'librosa'"

Если включён `FILTER_MUSIC=true`, нужна библиотека librosa:

```bash
pip install librosa
```

Фильтр будет работать и без librosa (в fallback-режиме), но с меньшей точностью.

### Проблема: VAD не распознаёт речь

**Решение**:
- Увеличь `AUDIO_VAD_AGGRESSIVENESS` в `.env` (1-3, по умолчанию 2)
- Уменьши фоновый шум
- Проверь уровень микрофона в системе

---

## 📊 Мониторинг

### Логи API

```bash
tail -f logs/api.log
```

### Логи Listener

Если запущен через nohup:
```bash
tail -f listener.log
```

### Метрики

```bash
python scripts/metrics_snapshot.py
cat cursor-metrics.json
```

---

## ✅ Чек-лист готовности

- [ ] Python ≥ 3.10 установлен
- [ ] FFmpeg установлен
- [ ] Зависимости установлены (`pip install -e ".[dev]"`)
- [ ] `.env` настроен
- [ ] API сервер запущен и отвечает на `/health`
- [ ] Listener запущен и видит микрофон
- [ ] После фразы появляются логи об отправке
- [ ] Транскрипция работает (если включена)

---

## 🎯 Следующие шаги

1. Настрой ночные триггеры (22:50 дайджест, 23:10 чек-ин)
2. Подключи Telegram-бота
3. Настрой Supabase для продакшена
4. Включи расширенные функции через `@playbook enhancement-plan`

---

**Готово! Reflexio 24/7 работает! 🎉**

