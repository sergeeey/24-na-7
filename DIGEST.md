# Reflexio Digest — Дневной дайджест

Автоматическая генерация ежедневных сводок из транскриптов с анализом информационной плотности.

---

## 🎯 Что это?

Reflexio Digest автоматически собирает все транскрипции за день, извлекает факты, анализирует информационную плотность и создаёт структурированный отчёт в формате Markdown или JSON.

---

## 🚀 Быстрый старт

### Через API

```bash
# Дайджест за сегодня (Markdown)
curl http://127.0.0.1:8000/digest/today

# Дайджест за конкретную дату (JSON)
curl http://127.0.0.1:8000/digest/2025-01-03?format=json

# Анализ информационной плотности
curl http://127.0.0.1:8000/digest/2025-01-03/density
```

### Через CLI

```bash
# Дайджест за сегодня
python scripts/generate_digest.py --date today

# Дайджест за вчера
python scripts/generate_digest.py --date yesterday

# Дайджест за конкретную дату
python scripts/generate_digest.py --date 2025-01-03

# Только анализ плотности
python scripts/generate_digest.py --date today --analyze-density --no-output-digest
```

### Через Playbook

```bash
@playbook digest-reflexio
```

С параметрами:

```bash
@playbook digest-reflexio --date yesterday --output_format json
```

---

## 📊 Что включает дайджест?

### 1. Метрики дня
- Количество транскрипций
- Общая длительность
- Количество слов/символов
- **Информационная плотность** (оценка 0-100)

### 2. Анализ информационной плотности

Уровни плотности:
- 🔴 **Очень высокая** (80-100) — очень продуктивный день
- 🟠 **Высокая** (60-80) — хороший день с активным обменом информацией
- 🟡 **Средняя** (40-60) — обычный день
- 🟢 **Низкая** (20-40) — спокойный день
- ⚪ **Очень низкая** (0-20) — минимальная активность

Компоненты анализа:
- **Плотность по времени** — сколько транскрипций на единицу времени
- **Плотность по объёму** — средняя длина транскрипций
- **Равномерность распределения** — как равномерно активность распределена по часам

### 3. Когнитивные метрики (расширенные)

Если включено `EXTENDED_METRICS=true`, дайджест включает дополнительный анализ:

- **Семантическая плотность** (0-1) — насколько насыщена речь смыслом
  - Комбинирует лексическое разнообразие, длину сегментов и плотность слов
  - 🔴 Очень высокая (≥0.7) — очень насыщенная речь с богатым словарём
  - 🟠 Высокая (≥0.5) — содержательная речь
  - 🟡 Средняя (≥0.3) — стандартная речь
  - 🟢 Низкая (≥0.15) — простая, повторяющаяся речь
  - ⚪ Очень низкая (<0.15) — минимальное содержание

- **Лексическое разнообразие** (0-1) — разнообразие словарного запаса
  - 1.0 = все слова уникальны
  - Высокое значение = богатый словарь

- **Скорость речи** (слов/мин) — средняя скорость речи
  - ≥180 — очень быстрая
  - 150-180 — быстрая
  - 120-150 — средняя
  - 90-120 — медленная
  - <90 — очень медленная

- **Вариация активности** — насколько неравномерно распределена активность по часам
  - Низкое значение = равномерная активность
  - Высокое значение = пиковая активность в определённое время

- **Метрики сегментации** — средняя длительность и длина сегментов

### 4. Извлечённые факты

Автоматическое извлечение фактов из транскрипций (в MVP — простое разбиение на предложения, в будущем — LLM-анализ).

### 5. Полные транскрипции (опционально)

Если включена опция `--include-metadata`, дайджест включает полные транскрипции с временными метками.

---

## 📁 Формат вывода

### Markdown (по умолчанию)

Сохранение: `digests/YYYY-MM-DD.md`

Пример структуры:
```markdown
# Reflexio Digest — 03 January 2025

## 📊 Метрики дня
- **Транскрипций:** 12
- **Фактов извлечено:** 24
- **Общая длительность:** 45.2 минут
- **Информационная плотность:** 72/100 (🟠 Высокая)

## 📝 Извлечённые факты
1. Первый факт...
2. Второй факт...
...
```

### JSON

Сохранение: `digests/YYYY-MM-DD.json`

Структура:
```json
{
  "date": "2025-01-03",
  "generated_at": "2025-01-03T22:50:00",
  "metrics": {
    "transcriptions_count": 12,
    "facts_count": 24,
    "information_density_score": 72.0,
    "density_level": "🟠 Высокая"
  },
  "facts": [...],
  "transcriptions": [...]
}
```

---

## 🔧 Настройка

### Включение расширенных метрик

В `.env` добавьте:

```bash
EXTENDED_METRICS=true
```

Расширенные метрики включают:
- Семантическую плотность
- Лексическое разнообразие
- Скорость речи (WPM)
- Вариацию активности
- Метрики сегментации

**Примечание**: Расширенные метрики требуют дополнительных вычислений, но работают локально без LLM.

### Минимальная уверенность фактов

```bash
python scripts/generate_digest.py --date today --min-confidence 0.7
```

(В MVP не используется, будет применяться в будущих версиях с LLM)

### Без метаданных

```bash
python scripts/generate_digest.py --date today --no-metadata
```

Это создаст более компактный дайджест без полных транскрипций.

---

## 🌙 Ночные триггеры

Для автоматической генерации дайджеста в 22:50 каждый день:

### Linux (cron)

```bash
# Добавить в crontab
crontab -e

# Дайджест в 22:50 каждый день
50 22 * * * cd /path/to/reflexio && /path/to/venv/bin/python scripts/generate_digest.py --date today
```

### Windows (Task Scheduler)

1. Открыть Task Scheduler
2. Создать базовую задачу
3. Триггер: ежедневно в 22:50
4. Действие: запуск программы
   - Программа: `C:\path\to\venv\Scripts\python.exe`
   - Аргументы: `scripts\generate_digest.py --date today`
   - Папка: `C:\path\to\reflexio`

### Через Enhancement Plan

```bash
@playbook enhancement-plan --enable_night_triggers true
```

Затем настроить Telegram-бота (в разработке).

---

## 📈 Примеры использования

### Ежедневный мониторинг

```bash
# Каждый вечер получать дайджест
python scripts/generate_digest.py --date today

# Проверять плотность
python scripts/generate_digest.py --date today --analyze-density --no-output-digest
```

### Сравнение дней

```bash
# Генерировать дайджесты за неделю
for i in {0..6}; do
  date=$(date -d "$i days ago" +%Y-%m-%d)
  python scripts/generate_digest.py --date $date
done
```

### API интеграция

```python
import requests

# Получить дайджест через API
response = requests.get("http://localhost:8000/digest/today?format=json")
digest = response.json()

print(f"Плотность: {digest['metrics']['information_density_score']}/100")
print(f"Фактов: {digest['metrics']['facts_count']}")
```

---

## 🎯 Будущие улучшения

- [ ] LLM-анализ для извлечения фактов и инсайтов
- [ ] Анализ эмоций по транскрипциям
- [ ] Автоматическое создание задач из фактов
- [ ] Сравнение дней и трендов
- [ ] Визуализация метрик (графики)
- [ ] Интеграция с Telegram-ботом для автоматической отправки

---

**Reflexio Digest превращает поток транскрипций в структурированное знание о дне.** 📊✨

